<div class="list_vertical">
  <header class="menu_header">
    <h1> Моё болото </h1>
    {{#if isEditor}}
      {{>headerEditor}}
    {{else}}
      {{>headerEditor}}
    {{/if}}
    Вы вошли как {{name}}
  </header>
  <br>
</div>